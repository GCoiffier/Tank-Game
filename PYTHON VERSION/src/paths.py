# Tank Game:
# A game with tanks and stuff.
#
# by Erkalys & Florimond Manca
#
# Paths constants file

import os

join = os.path.join

MAIN_PATH = os.getcwd()
MUSIC_PATH = join(MAIN_PATH,"music")
IMAGE_PATH = join(MAIN_PATH, "images")
TEXTURE_PATH = join(IMAGE_PATH, "textures")
LEVEL_PATH = join(MAIN_PATH, "levels")
CLEVEL_PATH = join(MAIN_PATH, "custom_levels")
